<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Shared.Domain — primitivas puras de domínio (Entity, AggregateRoot, ValueObject, Result,
    IDomainEvent, Actor). ZERO dependências externas exceto MediatR (IDomainEvent : INotification,
    desvio consciente herdado do delivery-app: dispatch de eventos sem wrapper próprio).
  -->
  <ItemGroup>
    <PackageReference Include="MediatR.Contracts" />
  </ItemGroup>

</Project>
